CREATE TABLE prosm_posts (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    title TEXT NOT NULL,
    modified TIMESTAMPTZ DEFAULT now() NOT NULL,
    user_id INTEGER
        REFERENCES prosm_users(id) ON DELETE CASCADE NOT NULL,
    content TEXT NOT NULL
);